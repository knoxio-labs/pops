/// Namespace for module-level facts. The design system itself is exposed as
/// tokens on `Color`/`Font`, the `Pops*` metric enums, and the state and layout
/// primitives — not through this type.
public enum DesignSystem {
    public static let moduleName = "DesignSystem"
}
