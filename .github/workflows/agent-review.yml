name: Agent Review

# Federation review gate: a row of deterministic structural guards,
# load-bearing and blocking. Nothing here is advisory.
#
# It used to also carry an advisory LLM reviewer as its last step, on the
# argument that the two halves shared a job but not authority — the guards
# decide whether a change may merge, the reviewer only comments. That argument
# was right, and it is why the reviewer now lives in `pr-review.yml` instead: a
# step that only comments should not be wedged inside a required context whose
# constraints it does not need and cannot satisfy. It could not be cancelled on
# a newer push, and it had to carry its draft condition at step level rather
# than skipping the job. See that file's header.
#
# The guards live here rather than in `quality.yml` because each is a Node
# script reading the source tree — one job, one checkout, nine invariants.
# Every guard runs its own `--self-test` first, so one that has quietly stopped
# catching anything fails the job instead of passing.
#
# THIS JOB IS TIER B and therefore installs the workspace. Four of the nine
# guards read structured config through a real parser — `check-mise-tool-
# overrides` and `check-node-pin` (TOML), `check-homelab-service-isolation`
# (compose YAML), `check-mise-setup-single-source` (workflow YAML) — and a
# parser needs `node_modules` on disk. The other five
# read JSON, TypeScript source or Markdown and would run fine without the
# install; they ride along because the tier is a property of the JOB, not of
# each guard, and splitting the job would split the required `agent-review`
# context along with it. Do NOT delete the install because the guard you are
# reading happens not to need it. The install-free Tier A jobs are the guard
# jobs in `quality.yml` that own exactly one guard each; the rule, and the list,
# are in docs/architecture/adr-045-guards-must-prove-they-report.md.
#
# RUNS ON DRAFTS TOO, and the job-level `if` that used to stop it is the reason.
# `agent-review` is a required context on `main`. A job-level `if` that
# evaluates false does not make the check absent, it makes it **skipped**, and
# GitHub's branch ruleset does not accept a skipped check run as a satisfied
# required check the way it accepts a workflow filtered out by paths. So every
# push made while a PR was a draft left the PR permanently BLOCKED.
#
# `gh pr ready` did not clear it. The `ready_for_review` event starts a second
# run that succeeds, and then the two views disagree — `gh pr checks` reports
# `agent-review pass` while `statusCheckRollup.contexts` still reports SKIPPED,
# and the ruleset believes the rollup. `gh pr merge` then refuses with "the base
# branch policy prohibits the merge" while every visible check is green. The
# only cures were close+reopen or another push, both of which re-run EVERY
# `pull_request` workflow — on a branch touching `clients/ios` that is a macOS
# job plus ~17 other checks, spent to clear one skipped check on a finished PR.
# `gh run rerun` cannot help: it replays the stored payload where `draft` was
# still true, so it skips again. Observed on #3944 and #3956.
#
# The fix is for the job to always run and conclude on its merits. The guards
# are cheap and a draft PR that violates a federation invariant is better off
# learning now than at ready-for-review, so they are not gated at all — and
# with the advisory reviewer gone there is no longer any step here that wants
# to be. Nothing in this job may acquire a condition: a step-level `if` is the
# most that was ever safe, and now not even that is needed.
#
# The property to keep: `agent-review`'s conclusion reflects the head it ran on,
# never the draft state at the time of some earlier push.

# RUNS ON MERGE GROUPS TOO, for the same reason it runs on drafts: `agent-review`
# is a required context, and a required context that never reports on the merge
# group's head holds that entry in the queue until the check-response timeout
# evicts it. The guards are identical there; only the one PR-shaped input moves.
# `github.base_ref` is empty outside a pull request, so the isolation litmus
# reads the merge group's own base instead.
#
# The other PR-shaped input was the LLM step's `--pr` number, and the shape of
# its condition is worth keeping in mind before adding any step that needs one:
# outside a pull request `github.event.pull_request` is null, and GitHub coerces
# both sides of `null == false` to 0 — so a draft test alone is TRUE on a merge
# group. Such a step must test `github.event_name` as well, which is why
# `pr-review.yml` carries no `merge_group:` trigger at all rather than a
# condition that has to get that right.

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:

# `pull-requests: write` left with the reviewer. Nothing in this job comments,
# and a required check that can write to a PR is a wider grant than it needs.
permissions:
  contents: read

jobs:
  agent-review:
    name: agent-review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Guard self-tests (prove the guards catch violations)
        run: |
          node scripts/ci/check-lib-no-pillar-import.mjs --self-test
          node scripts/ci/check-contract-isolation.mjs --self-test
          node scripts/ci/check-known-pillars-coverage.mjs --self-test
          node scripts/ci/check-mise-tool-overrides.mjs --self-test
          node scripts/ci/check-node-pin.mjs --self-test
          node scripts/ci/check-homelab-service-isolation.mjs --self-test
          node scripts/ci/check-docs-model.mjs --self-test
          node scripts/ci/check-tests-typechecked.mjs --self-test
          node scripts/ci/check-mise-setup-single-source.mjs --self-test
          # Not a guard over the source tree like the eight above — it is the
          # gate that decides whether the merge queue's macOS compile happens.
          # It self-tests in its own job too, immediately before it answers;
          # this preflight is what makes a broken scoper fail on the PR rather
          # than first inside a queue entry.
          node scripts/ci/merge-group-scope.mjs --self-test
      - name: Isolation litmus — no cross-contract reach-behind
        # `github.base_ref` is a pull-request-only field. On a merge group the
        # equivalent is `merge_group.base_ref`, which is a FULL ref
        # (`refs/heads/main`) where the other is a bare branch name — hence the
        # prefix strip rather than two spellings of the same command.
        env:
          BASE_REF: ${{ github.base_ref || github.event.merge_group.base_ref }}
        run: node scripts/ci/check-contract-isolation.mjs --base "origin/${BASE_REF#refs/heads/}"
      - name: Lib-never-imports-pillar guard
        run: node scripts/ci/check-lib-no-pillar-import.mjs
      - name: Known-pillars drift guard (PILLARS tuple vs on-disk data pillars)
        run: node scripts/ci/check-known-pillars-coverage.mjs
      - name: Mise toolchain override guard (root pin intact; only node/rust overridable per unit)
        run: node scripts/ci/check-mise-tool-overrides.mjs
      - name: Node pin coherence guard (mise/CI/engines/workflows/images agree on one major)
        run: node scripts/ci/check-node-pin.mjs
      - name: Homelab-service isolation guard (ADR-039 Inv 4 — HA/MQTT/Zigbee2MQTT/Matter never a pops service)
        run: node scripts/ci/check-homelab-service-isolation.mjs
      - name: Docs-model guard (ADR-041 — unit READMEs; no PRD/theme/ideas tree returns; every doc path a doc or comment names resolves)
        run: node scripts/ci/check-docs-model.mjs
      - name: Test-typecheck guard (a unit's tsconfig.json may not hide its own tests from tsc)
        run: node scripts/ci/check-tests-typechecked.mjs
      - name: Mise setup single-source guard (one pinned jdx/mise-action, and the pin retries its download)
        run: node scripts/ci/check-mise-setup-single-source.mjs
