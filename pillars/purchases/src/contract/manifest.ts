/**
 * Structural snapshot of the purchases pillar's public surface. The
 * contract type lives in `manifest.generated.ts`, regenerated by the
 * `generate:manifest` script. This file is the stable
 * `@pops/purchases/manifest` import subpath, so consumers don't track the
 * generator output.
 *
 * It also exports the runtime `purchasesManifest` value consumed when the
 * pillar self-registers with the registry.
 */
import type { ModuleManifest } from '@pops/types';

export type { PurchasesContract } from './manifest.generated.js';

export const purchasesManifest: ModuleManifest = {
  id: 'purchases',
  name: 'Purchases',
  version: '0.1.0',
  surfaces: ['app'],
  description: 'Purchase documents and line items, reconciled N:M against finance transactions.',
};
