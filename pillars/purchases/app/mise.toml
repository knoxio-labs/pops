[tasks.typecheck]
run = "tsc --noEmit"

[tasks.test]
run = ["node ../../../scripts/require-built-graph.mjs", "vitest run"]

[tasks.lint]
run = "oxlint src && oxfmt --check ."

[tasks.build]
run = "pnpm run build:remote"

[tasks."dev:standalone"]
description = "Run the purchases app on its own, with mock data"
run = "pnpm run dev:standalone"
