import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory bulk-entry page until its implementation ticket lands. */
export function BulkEntryPage() {
  return <PagePlaceholder title="Bulk entry" />;
}
