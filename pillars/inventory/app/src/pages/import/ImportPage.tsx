import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory import page until its implementation ticket lands. */
export function ImportPage() {
  return <PagePlaceholder title="Import" />;
}
