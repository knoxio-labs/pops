import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory moving-day page until its implementation ticket lands. */
export function MovingDayPage() {
  return <PagePlaceholder title="Moving day" />;
}
