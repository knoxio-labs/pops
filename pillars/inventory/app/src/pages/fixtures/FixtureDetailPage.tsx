import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory fixture detail page until its implementation ticket lands. */
export function FixtureDetailPage() {
  return <PagePlaceholder title="Fixture" />;
}
