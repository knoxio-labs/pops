import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory fixtures page until its implementation ticket lands. */
export function FixturesPage() {
  return <PagePlaceholder title="Fixtures" />;
}
