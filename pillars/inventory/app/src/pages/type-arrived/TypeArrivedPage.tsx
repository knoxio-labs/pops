import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory type-arrived page until its implementation ticket lands. */
export function TypeArrivedPage() {
  return <PagePlaceholder title="Type arrived" />;
}
