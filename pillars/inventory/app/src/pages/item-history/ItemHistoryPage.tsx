import { PagePlaceholder } from '../PagePlaceholder';

/** Inventory item history page until its implementation ticket lands. */
export function ItemHistoryPage() {
  return <PagePlaceholder title="History" />;
}
